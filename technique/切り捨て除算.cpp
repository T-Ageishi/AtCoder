#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, n) for (int i = 0; i < (n); i++)

/**
 * 切り捨て除算のやり方
 */
int main() {
  // ポイント1
  // 少数を使わない
  // C++のdoubleのような一般的な浮動小数点数型は
  // 高々(2進53桁)~(10進16桁)程度の精度しか持たない

  // ポイント2
  // 負数の除算に注意する
  // 負数の除算はいくつか定義があり、言語によって定義が異なる
}